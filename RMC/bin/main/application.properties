#For the first time use "ddl-auto=create" to auto create the table and insert default data.
#spring.jpa.hibernate.ddl-auto=create

#If table is already created and default data is present the use "ddl-auto=update".
spring.jpa.hibernate.ddl-auto=update

# MYSQL settings
spring.datasource.url=jdbc:mysql://localhost:3306/rmc_db
spring.datasource.username=root
spring.datasource.password=aabbcc
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

spring.jackson.serialization.fail-on-empty-beans=false